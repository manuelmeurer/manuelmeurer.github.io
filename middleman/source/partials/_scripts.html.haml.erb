- if build?

  :javascript
    // Redirect to canonical URL
    var origin = window.location.protocol + '//' + window.location.host;
    if (origin != "<%= config[:host] %>") {
      var correctUrl = window.location.href.replace(origin, "<%= config[:host] %>");
      window.location.replace(correctUrl);
    }
